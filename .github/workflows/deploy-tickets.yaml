name: deploy-tickets
on:
    push:
        branches: 
            - main 
        paths: 
            - 'tickets/**'
jobs: 
    build:
        runs-on: ubuntu-latest 
        steps: 
            - uses: actions/checkout@v2 
            - run: cd tickets && docker build -t nikapanchulidze/tickets .
            - 
              uses: docker/login-action@v1 
              with: 
                 username: ${{ secrets.DOCKER_USERNAME }}
                 password: ${{ secrets.DOCKERHUB_TOKEN }} 
            - run: docker push nikapanchulidze/tickets
